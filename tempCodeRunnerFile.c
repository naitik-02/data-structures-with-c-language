struct node* deleteAtend(struct node* head){
//     struct node* p = head;
//     struct node* q = head->next;
//     while(q->next != NULL){
//         p = p->next;
//         q = q->next;

//     }
//     p->next = q->next;
//     free(q);
//     return head;
//  }